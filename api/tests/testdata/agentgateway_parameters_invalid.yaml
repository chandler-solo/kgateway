_err: "minSeconds' value must be less than or equal to the 'maxSeconds"
apiVersion: agentgateway.dev/v1alpha1
kind: AgentgatewayParameters
metadata:
  name: shutdown-min-gt-max
spec:
  shutdown:
    minSeconds: 61
    maxSeconds: 60
---
_err: "AgentgatewayParameters/shutdown-min-gt-max/: spec.logging: Invalid value: \"object\": at most one of the fields in [level levels] may be set"
apiVersion: agentgateway.dev/v1alpha1
kind: AgentgatewayParameters
metadata:
  name: shutdown-min-gt-max
spec:
  logging:
    level: error
    levels:
      - rmcp=warn
      - hickory_server::server::server_future=off
      - typespec_client_core::http::policies::logging=warn
